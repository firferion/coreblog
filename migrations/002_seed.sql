INSERT INTO articles (title, slug, content, created_at, updated_at) VALUES
(
    'Как я переписывал ядро coreblog на Go три раза подряд и почему In-memory кэш — это лучшее решение для высоконагруженных систем в 2026 году',
    'rewriting-coreblog-three-times',
    'Это история о том, как стремление к совершенству заставило меня трижды пересмотреть архитектуру этого блога.

### Почему In-memory кэш?
1. **Скорость**: Доступ к RAM на порядки быстрее диска или даже Redis по сети.
2. **Простота**: `sync.RWMutex` и `map` — это всё, что нужно для базового решения.
3. **Надежность**: Меньше внешних зависимостей.

> "Программное обеспечение — это не только код, но и компромиссы".

Попробуйте обновить страницу и вы увидите, что контент отдается мгновенно!',
    NOW(), NOW()
),
(
    'Примеры кода в Go',
    'go-code-examples',
    'Давайте посмотрим на простой пример HTTP-сервера на Go 1.22+:

```go
package main

import (
    "fmt"
    "net/http"
)

func main() {
    mux := http.NewServeMux()
    mux.HandleFunc("GET /{$}", func(w http.ResponseWriter, r *http.Request) {
        fmt.Fprint(w, "Привет, мир!")
    })
    http.ListenAndServe(":8080", mux)
}
```

Этот код демонстрирует лаконичность стандартной библиотеки Go.',
    NOW() - INTERVAL '1 day', NOW() - INTERVAL '1 day'
),
(
    'Богатая разметка Markdown',
    'markdown-rich-content',
    'В нашем блоге поддерживаются следующие элементы:

* **Жирный текст** и *курсив*.
* Нумерованные и ненумерованные списки.
* [Ссылки на внешние ресурсы](https://go.dev).

#### Списки дел
- [x] Настроить PostgreSQL
- [x] Реализовать Store на Go
- [ ] Добавить поддержку комментариев

Markdown делает написание заметок приятным и быстрым процессом.',
    NOW() - INTERVAL '2 days', NOW() - INTERVAL '2 days'
),
(
    'Минимализм',
    'minimalism',
    'Короткая заметка для проверки того, как ведет себя "подвал" страницы при малом количестве контента на экране.

Ничего лишнего, только текст.',
    NOW() - INTERVAL '3 days', NOW() - INTERVAL '3 days'
),
(
    'Кейс: Очень длинная строка кода для проверки горизонтального скролла в блоке pre',
    'horizontally-scrolling-code',
    'В этом посте мы тестируем, как наш CSS справляется с очень длинными строками кода, которые не должны ломать верстку, а должны создавать горизонтальный скролл внутри блока.

```javascript
const veryLongString = "This is an extremely long string that we are using to test the horizontal scroll properties of our pre blocks in the Material Theme blog implementation which is built with Go and PostgreSQL and should handle this gracefully without breaking any layout components or causing unwanted overflow on the main page content area";
console.log(veryLongString.split("").reverse().join("").toUpperCase().repeat(5));
```

Если вы видите скролл — значит всё работает правильно!',
    NOW() - INTERVAL '4 days', NOW() - INTERVAL '4 days'
),
(
    'Тестирование таблиц в Markdown',
    'markdown-tables-test',
    'Проверяем рендеринг таблиц. В библиотеке `goldmark` по умолчанию таблицы могут быть не включены, если не добавлены расширения. Посмотрим, как это выглядит.

| Свойство | Значение | Описание |
| :--- | :---: | ---: |
| Язык | Go | Основной язык бэкенда |
| БД | Postgres | Реляционное хранилище данных |
| Кэш | Memory | Потокобезопасная map в оперативной памяти |
| UI | CSS | Чистая верстка без фреймворков |

Таблицы важны для технической документации.',
    NOW() - INTERVAL '5 days', NOW() - INTERVAL '5 days'
),
(
    'Глубоко вложенные списки и иерархия',
    'deeply-nested-lists',
    'Проверка отступов (`margin-left`) для многоуровневых списков.

1. Уровень 1
    * Уровень 2 (точка)
        * Уровень 3 (квадрат)
        * Еще один элемент 3 уровня
            1. Уровень 4 (число)
            2. Уровень 4 продолжение
    * Назад на уровень 2
2. Завершение уровня 1

Это помогает понять, насколько хорошо читается сложная структура текста.',
    NOW() - INTERVAL '6 days', NOW() - INTERVAL '6 days'
),
(
    'Заголовок статьи, который настолько длинный, что он обязательно должен занять как минимум четыре или пять строк на мобильных устройствах и даже на десктопе, создавая серьезное испытание для нашего CSS Grid и вертикальных отступов',
    'giant-title-test',
    'Этот пост нужен исключительно для того, чтобы проверить, как верстка карточки статьи на главной странице справляется с огромным количеством текста в теге `h2`. 

Благодаря Flexbox и `margin-top: auto` для даты, карточка должна оставаться аккуратной, даже если заголовок занимает половину экрана. Сниппет текста должен корректно обрезаться после 3 строк, не накладываясь на заголовок.',
    NOW() - INTERVAL '7 days', NOW() - INTERVAL '7 days'
),
(
    'Оптимизация производительности Go-приложений',
    'go-performance-tips',
    'Несколько советов по ускорению вашего бэкенда:
* Используйте `sync.Pool` для часто создаваемых объектов.
* Избегайте лишних аллокаций в горячих циклах.
* Профилируйте код с помощью `pprof`.

Производительность — это вежливость программиста.',
    NOW() - INTERVAL '8 days', NOW() - INTERVAL '8 days'
),
(
    'Будущее веб-разработки в 2026 году',
    'future-web-2026',
    'Мы видим возвращение к истокам:
- Серверный рендеринг (SSR).
- Уменьшение зависимости от тяжелых JS-фреймворков.
- Рост популярности языков со строгой типизацией на бэкенде.

DevLog следует этим трендам!',
    NOW() - INTERVAL '9 days', NOW() - INTERVAL '9 days'
);

INSERT INTO tags (name, slug) VALUES ('Go', 'go'), ('Backend', 'backend'), ('PostgreSQL', 'postgres') ON CONFLICT DO NOTHING;
INSERT INTO article_tags (article_id, tag_id) VALUES (1, 1), (1, 2), (2, 1), (3, 2), (5, 1), (6, 3) ON CONFLICT DO NOTHING;
