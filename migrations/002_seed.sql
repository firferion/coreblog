INSERT INTO articles (title, slug, content, created_at, updated_at) VALUES
(
    'Как я переписывал ядро coreblog на Go три раза подряд и почему In-memory кэш — это лучшее решение для высоконагруженных систем в 2026 году',
    'rewriting-coreblog-three-times',
    'Это история о том, как стремление к совершенству заставило меня трижды пересмотреть архитектуру этого блога.

### Почему In-memory кэш?
1. **Скорость**: Доступ к RAM на порядки быстрее диска или даже Redis по сети.
2. **Простота**: `sync.RWMutex` и `map` — это всё, что нужно для базового решения.
3. **Надежность**: Меньше внешних зависимостей.

> "Программное обеспечение — это не только код, но и компромиссы".

Попробуйте обновить страницу и вы увидите, что контент отдается мгновенно!',
    NOW(), NOW()
),
(
    'Примеры кода в Go',
    'go-code-examples',
    'Давайте посмотрим на простой пример HTTP-сервера на Go 1.22+:

```go
package main

import (
    "fmt"
    "net/http"
)

func main() {
    mux := http.NewServeMux()
    mux.HandleFunc("GET /{$}", func(w http.ResponseWriter, r *http.Request) {
        fmt.Fprint(w, "Привет, мир!")
    })
    http.ListenAndServe(":8080", mux)
}
```

Этот код демонстрирует лаконичность стандартной библиотеки Go.',
    NOW() - INTERVAL '1 day', NOW() - INTERVAL '1 day'
),
(
    'Богатая разметка Markdown',
    'markdown-rich-content',
    'В нашем блоге поддерживаются следующие элементы:

* **Жирный текст** и *курсив*.
* Нумерованные и ненумерованные списки.
* [Ссылки на внешние ресурсы](https://go.dev).

#### Списки дел
- [x] Настроить PostgreSQL
- [x] Реализовать Store на Go
- [ ] Добавить поддержку комментариев

Markdown делает написание заметок приятным и быстрым процессом.',
    NOW() - INTERVAL '2 days', NOW() - INTERVAL '2 days'
),
(
    'Минимализм',
    'minimalism',
    'Короткая заметка для проверки того, как ведет себя "подвал" страницы при малом количестве контента на экране.

Ничего лишнего, только текст.',
    NOW() - INTERVAL '3 days', NOW() - INTERVAL '3 days'
);
